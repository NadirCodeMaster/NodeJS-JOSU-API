---
components:
  schemas:
    Person:
      type: object
      properties:
        id:
          type: string
        user_id:
          type: string
        account_id:
          type: string
        confidence_is_person:
          type: number
          nullable: true
        is_person:
          type: boolean
          nullable: true
      required:
        - id
        - user_id
        - account_id
        - confidence_is_person
        - is_person
